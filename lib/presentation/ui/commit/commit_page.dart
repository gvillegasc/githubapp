import 'package:flutter/material.dart';

class CommitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CommitPage'),
      ),
    );
  }
}
